@import url('../../styles/appStyles.pcss');

.message-area {
  height: 600px;
  width: 550px;
}

.message-area__not-selected-label {
  font-family: $globalFontFamily;
  font-size: 24px;
  color: $userCellMessegeColor;
  opacity: 0.4;
}
