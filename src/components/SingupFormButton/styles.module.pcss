@import "../../styles/appStyles.pcss";

.singupform__button {
  height: 3.5rem;
  display: block;
  width: 100%;
  margin-top: 20px;
  margin-bottom: 10px;
  font-family: inherit;
  border: 0px solid;
  border-radius: 10px;
  background-color: $buttonsColor;
  color: $buttonTextColor;
  font-size: 20px;
}
