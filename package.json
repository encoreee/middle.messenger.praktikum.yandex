{
  "name": "messenger",
  "version": "1.0.0",
  "license": "MIT",
  "browserslist": "> 0.5%, last 2 versions, not dead",
 
  "scripts": {
    "dev": "parcel src/index.hbs src/*.hbs--no-cache",
    "build": "parcel build src/index.hbs src/views/*.hbs --no-cache",
    "test": "echo \"Error: no test sprcified\" && exit 1"
  },
  "dependencies": {
    "@anikin/parcel-transformer-handlebars": "^1.0.9",
    "handlebars": "^4.7.7",
    "parcel": "^2.8.3"
  }
}
